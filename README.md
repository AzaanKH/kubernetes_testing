# kubernetes_testing, laptop single node cluster

# Docker Setup
1. Install Docker Desktop
2. Check docker version
```bash
docker --version
```
3. Check kubelet version
```bash
kubelet --version --client=true -o yaml
```

